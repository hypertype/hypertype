{
  "name": "@hypertype/hypertype",
  "version": "2.1.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b --watch",
    "bundle": "lerna run bundle:prod",
    "publish": "lerna publish patch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "dependencies": {},
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
